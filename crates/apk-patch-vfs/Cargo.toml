[package]
name = "apk-patch-vfs"
description = "Virtual filesystem for apk-patch decode/build (StdFs + MemVfs)"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
thiserror = { workspace = true }
walkdir = { workspace = true }

[features]
default = ["native-fs"]
native-fs = []
